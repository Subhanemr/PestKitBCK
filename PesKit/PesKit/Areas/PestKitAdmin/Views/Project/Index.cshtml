@model List<Project>
@{
    int count = 0;
}
<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Project table</h4>
            <a asp-controller="Project" asp-action="Create" class="btn btn-warning fw-bold fs-6 text-white">Create Project</a>
            <div class="table-responsive pt-3">
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>
                                #
                            </th>
                            <th>
                                Image
                            </th>
                            <th>
                                Title
                            </th>
                            <th>
                                Settings
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (Project project in Model)
                        {
                            count++;
                            <tr>
                                <td>
                                    @count
                                </td>
                                <td>
                                    <img src="~/img//@project.ProjectImages.FirstOrDefault(p => p.IsPrimary==true)?.Url" />
                                </td>
                                <td>
                                    @project.Name
                                </td>
                                <td>
                                    <a asp-action="Update" asp-controller="Blog" asp-route-id="@project.Id" class="btn btn-primary btn-info">
                                        <i class="mdi mdi-pencil"></i>
                                    </a>
                                    <a asp-action="Delete" asp-controller="Blog" asp-route-id="@project.Id" class="btn btn-danger btn-info">
                                        <i class="mdi mdi-delete"></i>
                                    </a>
                                    <a asp-action="More" asp-controller="Blog" asp-route-id="@project.Id" class="btn btn-primary">
                                        <i class="mdi mdi-open-in-new"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>